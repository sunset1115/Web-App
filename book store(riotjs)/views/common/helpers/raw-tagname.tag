<raw-tagname>
	<script>
		// dynamically load a tag based solely on it's name
		// very useful for svgs tags!!
		if (opts.name != undefined){
			riot.mount(this.root, opts.name)
		}
	</script>
</raw-tagname>